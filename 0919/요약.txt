이차원 배열  null을 포함해 n+1

랜덤 없으면
for(i=0; i<10; i++)
{
    do
    {
        rnd = rand() % 10;
    } while(rnd == i);
    temp = a[rnd];
    a[rnd] = a[i];
    a[i] = temp;
}
활용

아스키 코드에드 음성이 구현되어 있음

Visualgo 사이트 참고 및 활용하기

배열을 사용할 때 가장 큰 문제는 배열의 크기를 미리 정해야 한다는 것
array와 linked list 구분할것

피아노-동요플레이(음계), 카드-셔플, 자료구조-스택 구현(push,pop 함수 구현 및 테스트) 
-------------------------------
발표
코파일럿-co-pilot
vscode 확장을 통해 github 코파일럿을 추가할 수 있음

